import React from 'react';
import Nav from '../Nav';

function Header() {
    const [currentCategory, setCurrentCategory] = useState(categories[0]);
    besrsaersearsea

    return (
        <header className='flex-row px-1'>
            <h1>Nick Haretakis</h1> <Nav></Nav>
        </header>
    )
}

export default Header;